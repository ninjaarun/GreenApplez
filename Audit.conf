<Resource>
<ID>Audit</ID>
<PropertyBaseName>com.novell.zenworks.audit.web.resources.Audit</PropertyBaseName>
</Resource>
  for (int v = 0; v < V; v++)  
        if (mstSet[v] == false && key[v] < min)  
            min = key[v], min_index = v;  
  
    return min_index;  
    
asda
